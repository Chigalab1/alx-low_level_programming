#include "main.h"

/**
 * _abs - function that finds absolute value of an int
 * @n: parameter to check
 *
 * Return: unsigned int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
